description: "YOLO12n, AdamW, imgsz=1024, lr0=0.000438, batch=16 (tune_v2 80ep trial_046 最佳參數)"

model: model/yolo12n.yaml

train:
  epochs:        80
  batch_size:    16
  learning_rate: 0.000438
  optimizer:     AdamW
  patience:      25
  img_size:      1024
  device:        "0"
  seed:          0
  workers:       2
