description: "YOLO12n, AdamW, imgsz=1024, lr0=0.000676, batch=16, ABC 自適應 CLAHE 前處理 (基於 exp006/trial_040)"

model: model/yolo12n.yaml
dataset: dataset_clahe_abc.yaml

train:
  epochs:        80
  batch_size:    16
  learning_rate: 0.000676
  optimizer:     AdamW
  patience:      25
  img_size:      1024
  device:        "0"
  seed:          0
  workers:       2
