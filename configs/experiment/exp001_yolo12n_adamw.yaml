description: "YOLO12n, AdamW, imgsz=1024, lr0=0.0014"

preprocess: preprocess/default.yaml
model:      model/yolo12n.yaml

train:
  epochs:        80
  batch_size:    16
  learning_rate: 0.0014
  optimizer:     AdamW
  patience:      25
  img_size:      1024
  device:        auto
